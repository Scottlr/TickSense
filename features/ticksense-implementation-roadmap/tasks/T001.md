### T001 - Plugin Hub Gradle Skeleton

#### Goal
Create a Plugin Hub-compatible Java 11 RuneLite external-plugin skeleton for TickSense.

#### Scope
- Create Gradle wrapper files from the official RuneLite external plugin template.
- Create `settings.gradle`, `build.gradle`, `runelite-plugin.properties`, and `README.md`.
- Create package root `src/main/java/com/ticksense/runelite/`.
- Create minimal `TickSensePlugin` with `@PluginDescriptor(name = "TickSense")`.
- Create `src/test/java/com/ticksense/runelite/` with only template-compatible verification if the template includes it.

#### Out of scope
- No event capture, telemetry model, storage, analytics, activities, overlays, or debug recorder.
- Do not add any non-template runtime dependency, subprocess hook, or downloaded code path.

#### Modules touched
- `settings.gradle`
- `build.gradle`
- `gradlew`, `gradlew.bat`, `gradle/wrapper/*`
- `runelite-plugin.properties`
- `src/main/java/com/ticksense/runelite/TickSensePlugin.java`
- `README.md`

#### Dependencies
None.

#### Related Code
- `docs/ARCHITECTURE.md` sections 5, 8, 9, 22, and 23.
- Planned `com.ticksense.runelite.TickSensePlugin`.

#### Implementation Touchpoints
- Read first: `docs/ARCHITECTURE.md` sections "RuneLite Development Environment", "Package and Module Structure", and "MVP Definition".
- Edit first: root Gradle/template files, then `TickSensePlugin.java`.
- Symbols to search: `PluginDescriptor`, `runelite-plugin.properties`, `Plugin Hub`, `Java 11`.
- Required structural changes: one Java 11 external-plugin module using package `com.ticksense`.
- Do not edit: `features/ticksense-implementation-roadmap/**` except to record completion notes after implementation.
- Verify in order: `./gradlew test`, then `./gradlew run`.
- Stop and ask: if the official external plugin template has materially changed Java target, descriptor shape, or Plugin Hub metadata requirements.

#### Implementation Notes
- Use Java 11 language features only; do not use records, sealed classes, pattern matching, or newer APIs.
- `TickSensePlugin` should be intentionally empty beyond lifecycle method stubs needed by the template.
- `runelite-plugin.properties` must point at `com.ticksense.runelite.TickSensePlugin`.
- `README.md` should state that TickSense is retrospective execution analytics and not a live helper.

Expected skeleton:

```java
package com.ticksense.runelite;

import com.google.inject.Provides;
import lombok.extern.slf4j.Slf4j;
import net.runelite.client.config.ConfigManager;
import net.runelite.client.plugins.Plugin;
import net.runelite.client.plugins.PluginDescriptor;

@Slf4j
@PluginDescriptor(name = "TickSense")
public class TickSensePlugin extends Plugin
{
    @Override
    protected void startUp()
    {
    }

    @Override
    protected void shutDown()
    {
    }
}
```

#### Before / After
Before:

```text
docs/ARCHITECTURE.md
features/ticksense-implementation-roadmap/
```

After:

```text
build.gradle
settings.gradle
runelite-plugin.properties
src/main/java/com/ticksense/runelite/TickSensePlugin.java
```

#### Migration Notes
- No existing production APIs exist, so there is no compatibility migration.
- Keep the initial branch simple enough that later tasks can add packages without renaming the plugin root.

#### Misinterpretations To Prevent
- Wrong interpretation: start by building analytics models in the plugin class.
  Correct rule: T001 creates a bootable shell only.
- Wrong interpretation: use a newer Java feature because the local JDK supports it.
  Correct rule: write Java 11-safe source for Plugin Hub compatibility.
- Wrong interpretation: add a local app or standalone launcher to prove behavior.
  Correct rule: use the RuneLite external plugin template and Gradle `run` task only.

#### Invariants
- `TickSensePlugin` remains a thin RuneLite entrypoint.
- No MVP analytics behavior exists in this task.
- The project can be built and run with the external plugin Gradle workflow.

#### Acceptance Criteria
- [ ] `./gradlew test` completes successfully.
- [ ] `./gradlew run` boots RuneLite with TickSense visible as a plugin.
- [ ] `runelite-plugin.properties` references `com.ticksense.runelite.TickSensePlugin`.
- [ ] No package outside `com.ticksense.runelite` contains production code yet.

#### Tests Required
- No behavior unit tests are required unless the official template includes a meaningful generated test.
- Run `./gradlew test`.

#### Agent Handoff Context
This is the foundation for all later tasks. Keep it boring, template-compatible, and easy to review. The product architecture starts in later PRs.
