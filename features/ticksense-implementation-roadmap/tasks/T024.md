### T024 - Construction Registry and Verification Fixtures

#### Goal
Verify Construction menu/widget/inventory evidence and create source-owned primitive ID registries and fixtures.

#### Scope
- Create `ConstructionIds` for verified widget, object, item, animation, and menu evidence.
- Add normalized fixture placeholders for one approved Construction method.
- Verify menu open, option click, build/remove confirmation, inventory change, XP confirmation, and banking/servant evidence.

#### Out of scope
- No Construction strategy, analyzer, or UI changes.
- No menu entry swaps, generated input, widget automation, servant automation, or live menu advice.
- No broad multi-method abstraction.

#### Modules touched
- `src/main/java/com/ticksense/activities/construction/ConstructionIds.java`
- `src/main/java/com/ticksense/activities/construction/ConstructionVerificationDecision.java`
- `src/test/resources/replays/construction-basic.jsonl`
- `src/test/java/com/ticksense/activities/construction/ConstructionIdsTest.java`

#### Dependencies
T006, T010, T020.

#### Related Code
- `docs/ARCHITECTURE.md` sections 2, 7, 12, 15, and 20.
- Planned `ConstructionIds`.

#### Implementation Touchpoints
- Read first: architecture "Construction menu efficiency" and "ID registry pattern".
- Edit first: `ConstructionIds.java`.
- Symbols to search: `MenuOpened`, `WidgetTelemetryEvent`, `InventoryDeltaTelemetryEvent`, `StatChangedTelemetryEvent`.
- Required structural changes: primitive ID registry and verification decision for one method.
- Do not edit: RuneLite menu entries or use menu swapper behavior.
- Verify in order: `./gradlew test --tests "*ConstructionIdsTest"`, `./gradlew test`.
- Stop and ask: if the first Construction method is not approved.

#### Implementation Notes
- `ConstructionIds` imports no `net.runelite.*`.
- Verify one method before expanding to multiple build/remove flows.

#### Before / After
Before:

```text
No Construction package exists.
```

After:

```java
ConstructionVerificationDecision decision = ConstructionVerificationDecision.verified("oak-larder", ...);
```

#### Migration Notes
- Widget IDs are content/version sensitive; keep verification date comments.
- Fixture updates should accompany registry changes.

#### Misinterpretations To Prevent
- Wrong interpretation: observing menus allows menu mutation.
  Correct rule: observe menus/widgets only.
- Wrong interpretation: a registry for every Construction method is needed now.
  Correct rule: verify one method first.

#### Invariants
- No generated input or menu mutation.
- Registry imports no RuneLite classes.
- Post-MVP work stays blocked by T020.

#### Acceptance Criteria
- [x] Construction registry exists for one approved method.
- [x] Verification decision names the method and evidence.
- [x] Fixture README explains capture/anonymization.
- [x] `./gradlew test` passes.

#### Tests Required
- `ConstructionIdsTest.registryImportsNoRuneliteClasses()`.
- `ConstructionIdsTest.enabledStrategyRequiresVerifiedMethodIds()`.

#### Agent Handoff Context
Construction is compliance-sensitive because menus are involved. Keep the verification evidence narrow and observe-only.

#### Completion Notes
- Completed on branch `codex/t024-construction-registry-and-verification-fixtures`.
- Added `ConstructionIds` for the approved `oak-larder` method with source-owned primitive object, item, animation, and bank-widget evidence pulled from the pinned RuneLite API constants already used by the project.
- Added `ConstructionVerificationDecision` with explicit `VERIFIED`, `PARTIALLY_VERIFIED`, and `BLOCKED` states plus method name, review date, evidence, blockers, and notes so downstream Construction work can inspect one reviewed gate.
- Kept the current decision at `PARTIALLY_VERIFIED` because the repo still lacks sanitized normalized replay evidence for menu-open timing, build/remove clicks, construction-widget confirmation IDs, inventory/XP confirmation, and servant refill behavior.
- Reserved `src/test/resources/replays/construction-basic.jsonl` as the normalized fixture placeholder and documented capture/anonymization requirements in `src/test/resources/replays/README.md`.
- Added `ConstructionIdsTest` to protect the no-`net.runelite` import rule, verify the approved method and primitive IDs, and ensure the replay README names the Construction fixture and sanitization guidance.
- Validation:
  - `./gradlew test --tests "*ConstructionIdsTest"` passed under Java 11.
  - `./gradlew test` passed under Java 11.
