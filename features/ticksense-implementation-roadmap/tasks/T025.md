### T025 - Vardorvis Registry and Verification Fixtures

#### Goal
Verify Vardorvis projectile, graphic, animation, damage, and arena evidence before any strategy reports mechanic timings.

#### Scope
- Create `VardorvisIds` with primitive IDs and source comments.
- Add normalized fixture placeholders for `vardorvis-basic.jsonl`.
- Verify ranged-head, blood-splat, axe, prayer-response evidence if visible, and damage attribution evidence.

#### Out of scope
- No Vardorvis strategy or analyzer.
- No projectile recoloring or alteration.
- No live mechanic solver, prayer/tile prompts, or warnings.

#### Files touched
- `src/main/java/com/ticksense/activities/vardorvis/VardorvisIds.java`
- `src/main/java/com/ticksense/activities/vardorvis/VardorvisVerificationDecision.java`
- `src/test/resources/replays/vardorvis-basic.jsonl`
- `src/test/java/com/ticksense/activities/vardorvis/VardorvisIdsTest.java`

#### Dependencies
T006, T010, T020.

#### Related Code
- `docs/ARCHITECTURE.md` sections 2, 6, 7, 15, 20, and 24.
- Planned `VardorvisIds`.

#### Implementation Touchpoints
- Read first: architecture "Vardorvis mechanic response" and open question "How reliable is ProjectileMoved for each boss mechanic?".
- Edit first: `VardorvisIds.java`.
- Symbols to search: `ProjectileTelemetryEvent`, `GraphicTelemetryEvent`, `AnimationTelemetryEvent`, `DamageTelemetryEvent`.
- Required structural changes: primitive ID registry and fixture-backed verification decision.
- Do not edit: live rendering or projectile visuals.
- Verify in order: `./gradlew test --tests "*VardorvisIdsTest"`, `./gradlew test`.
- Stop and ask: if projectile/graphic reliability is not proven by captured timelines.

#### Implementation Notes
- `VardorvisVerificationDecision.Status`: `VERIFIED`, `PARTIALLY_VERIFIED`, `BLOCKED`.
- Only `VERIFIED` mechanics may be used by T034 in normal reports.

#### Before / After
Before:

```text
No Vardorvis package exists.
```

After:

```java
VardorvisVerificationDecision decision = VardorvisVerificationDecision.partial(...);
```

#### Migration Notes
- Existing Vardorvis-related plugins may inform IDs, but TickSense must not alter projectiles.
- Update golden fixtures when mechanics or IDs change.

#### Misinterpretations To Prevent
- Wrong interpretation: adapting a projectile plugin means changing visuals.
  Correct rule: TickSense observes projectiles only.
- Wrong interpretation: partial verification can drive normal mechanic reports.
  Correct rule: unverified mechanics are omitted or debug-only.

#### Invariants
- Registry imports no RuneLite classes.
- All mechanic triggers are verified before normal reports.
- No live solver behavior.

#### Acceptance Criteria
- [x] Vardorvis registry exists with primitive IDs and source comments.
- [x] Verification decision records which mechanics are verified.
- [x] Fixture README explains capture/anonymization.
- [x] `./gradlew test` passes.

#### Tests Required
- `VardorvisIdsTest.registryImportsNoRuneliteClasses()`.
- `VardorvisIdsTest.normalReportsRequireVerifiedMechanicIds()`.

#### Agent Handoff Context
This is a verification-heavy extension. Do not let inferred IDs become product behavior.

#### Completion Notes
- Completed on branch `codex/t025-vardorvis-registry-and-verification-fixtures`.
- Added `VardorvisIds` with source-owned primitive boss/head NPC IDs and explicitly empty projectile, graphic, axe, and arena registries until normalized replay fixtures can prove those mechanic IDs.
- Added `VardorvisVerificationDecision` with `VERIFIED`, `PARTIALLY_VERIFIED`, and `BLOCKED` states plus explicit verified-mechanic and unresolved-mechanic lists so downstream Vardorvis work can inspect one reviewed gate.
- Kept the current decision at `PARTIALLY_VERIFIED` because the repository still lacks sanitized normalized replay evidence for ranged-head projectile timing, blood-splat graphics, axe cues, prayer-response evidence, damage attribution windows, and arena scoping.
- Reserved `src/test/resources/replays/vardorvis-basic.jsonl` as the normalized fixture placeholder and documented capture/anonymization requirements in `src/test/resources/replays/README.md`.
- Added `VardorvisIdsTest` to protect the no-`net.runelite` import rule, verify the source-owned NPC IDs, and ensure the replay README names the Vardorvis fixture and sanitization guidance.
- Validation:
  - `./gradlew test --tests "*VardorvisIdsTest"` passed under Java 11.
  - `./gradlew test` passed under Java 11.
