### T026 - Inferno Registry and Verification Fixtures

#### Goal
Verify Inferno wave, NPC, projectile/animation, prayer-state, supply, and death evidence before building retrospective analytics.

#### Scope
- Create `InfernoIds` with primitive IDs and source comments.
- Add normalized fixture placeholders for `inferno-wave-basic.jsonl`.
- Verify region/wave evidence, nibbler evidence, prayer-state evidence if available, supply usage evidence, and death timeline evidence.

#### Out of scope
- No Inferno strategy or analyzer.
- No live solver, tile/prayer/target prescriptions, overlays, or wave advice.
- No unverified prayer timing model.

#### Modules touched
- `src/main/java/com/ticksense/activities/inferno/InfernoIds.java`
- `src/main/java/com/ticksense/activities/inferno/InfernoVerificationDecision.java`
- `src/test/resources/replays/inferno-wave-basic.jsonl`
- `src/test/java/com/ticksense/activities/inferno/InfernoIdsTest.java`

#### Dependencies
T006, T010, T020.

#### Related Code
- `docs/ARCHITECTURE.md` sections 2, 4, 7, 15, 20, and 24.
- Planned `InfernoIds`.

#### Implementation Touchpoints
- Read first: architecture "Inferno prayer flick consistency" and "Inferno" metrics.
- Edit first: `InfernoIds.java`.
- Symbols to search: `NpcStateTelemetryEvent`, `ProjectileTelemetryEvent`, `AnimationTelemetryEvent`, `InventoryDeltaTelemetryEvent`.
- Required structural changes: primitive ID registry and verification decision.
- Do not edit: UI to show live wave solution.
- Verify in order: `./gradlew test --tests "*InfernoIdsTest"`, `./gradlew test`.
- Stop and ask: if prayer state/timing cannot be verified safely.

#### Implementation Notes
- Prayer timing remains unavailable unless the verification decision marks prayer evidence `VERIFIED`.
- Nibbler response can be verified independently from prayer timing.

#### Before / After
Before:

```text
No Inferno package exists.
```

After:

```java
InfernoVerificationDecision decision = InfernoVerificationDecision.verifiedWaveEvidence(...);
```

#### Migration Notes
- Strategy should be disabled for unverified prayer timing features.
- Golden fixtures should identify wave and attempt schema versions.

#### Misinterpretations To Prevent
- Wrong interpretation: Inferno analytics should help solve the current wave.
  Correct rule: analysis is retrospective after wave/attempt completion.
- Wrong interpretation: prayer timing can be inferred without verified prayer state.
  Correct rule: verify prayer API/varbits before reporting timing.

#### Invariants
- Registry imports no RuneLite classes.
- No live solver behavior.
- Unverified prayer timing stays out of normal reports.

#### Acceptance Criteria
- [x] Inferno registry exists with primitive IDs and source comments.
- [x] Verification decision separates wave, nibbler, prayer, supply, and death evidence.
- [x] Fixture README explains capture/anonymization.
- [x] `./gradlew test` passes.

#### Tests Required
- `InfernoIdsTest.registryImportsNoRuneliteClasses()`.
- `InfernoIdsTest.prayerTimingRequiresVerifiedPrayerEvidence()`.

#### Agent Handoff Context
Inferno has the highest risk of becoming a solver. This task creates only verified evidence gates.

#### Completion Notes
- Completed on branch `codex/t026-inferno-registry-and-verification-fixtures`.
- Added `InfernoIds` with source-owned primitive nibbler, wave-NPC, and common-supply item IDs while keeping region, prayer-state, and death-timeline registries explicitly empty until normalized replay fixtures can verify them.
- Added `InfernoVerificationDecision` with per-evidence statuses for wave, nibbler, prayer, supply, and death evidence so downstream Inferno work can inspect exactly which areas remain blocked.
- Kept the current decision at `PARTIALLY_VERIFIED` because the repository still lacks sanitized normalized replay evidence for wave boundaries, prayer state/timing, supply-usage timing, and death timelines.
- Reserved `src/test/resources/replays/inferno-wave-basic.jsonl` as the normalized fixture placeholder and documented capture/anonymization requirements in `src/test/resources/replays/README.md`.
- Added `InfernoIdsTest` to protect the no-`net.runelite` import rule, verify the source-owned Inferno IDs, and ensure prayer timing stays disabled until prayer evidence becomes verified.
- Validation:
  - `./gradlew test --tests "*InfernoIdsTest"` passed under Java 11.
  - `./gradlew test` passed under Java 11.
