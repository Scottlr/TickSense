### T029 - Araxxor Verification Capture Fixtures

#### Goal
Capture and document normalized Araxxor verification fixtures, or keep Araxxor blocked from normal reports when spider triggers cannot be verified.

#### Scope
- Add anonymized replay fixture placeholders and capture instructions for `araxxor-spider-basic.jsonl` and `araxxor-teleport-midkill.jsonl`.
- Add `AraxxorVerificationDecision` with statuses `VERIFIED`, `PARTIALLY_VERIFIED`, and `BLOCKED`.
- Verify boss NPC IDs, spider NPC IDs, spider availability evidence, attack click evidence, interaction evidence, damage evidence, and termination evidence.
- Record the verification source and date in `src/test/resources/replays/README.md`.

#### Out of scope
- No Araxxor strategy implementation; T015 owns strategy behavior.
- No live overlays, target highlighting, or mechanic prompts.
- No arbitrary user-supplied IDs.

#### Files touched
- `src/main/java/com/ticksense/activities/araxxor/AraxxorVerificationDecision.java`
- `src/test/resources/replays/araxxor-spider-basic.jsonl`
- `src/test/resources/replays/araxxor-teleport-midkill.jsonl`
- `src/test/resources/replays/README.md`
- `src/test/java/com/ticksense/activities/araxxor/AraxxorVerificationFixtureTest.java`

#### Dependencies
T010, T014, T020.

#### Related Code
- `docs/ARCHITECTURE.md` sections 7, 11, 15, 20, 22, and 24.
- Planned `AraxxorVerificationDecision`.

#### Implementation Touchpoints
- Read first: architecture "Araxxor strategy", "ID registry pattern", and open question "Can Araxxor spider attackability be detected directly?".
- Edit first: `src/test/resources/replays/README.md`, then `AraxxorVerificationDecision.java`.
- Symbols to search: `ARAXXOR_NPC_IDS`, `SPIDER_NPC_IDS`, `NpcStateTelemetryEvent`, `InteractingChangedTelemetryEvent`, `DamageTelemetryEvent`.
- Required structural changes: fixture-backed verification decision that downstream tasks can inspect.
- Do not edit: `AraxxorStrategy.java` except feature-gate references if it already exists.
- Verify in order: `./gradlew test --tests "*AraxxorVerificationFixtureTest"`, `./gradlew test`.
- Stop and ask: if verification data contains player names, chat, or account-identifying information that cannot be anonymized.

#### Implementation Notes
- `AraxxorVerificationDecision` fields:

```java
public final class AraxxorVerificationDecision
{
    public enum Status { VERIFIED, PARTIALLY_VERIFIED, BLOCKED }

    private final Status status;
    private final String verifiedOnDate;
    private final List<String> evidence;
    private final List<String> unresolvedQuestions;
}
```

- `BLOCKED` is a valid completed outcome for this task when Araxxor cannot be verified safely. T015 must then keep Araxxor out of normal reports instead of inventing behavior.

#### Before / After
Before:

```text
Araxxor ID gates exist, but no captured timeline proves spider opportunity evidence.
```

After:

```java
AraxxorVerificationDecision decision = AraxxorVerificationDecision.verified(...);
```

#### Migration Notes
- Captured fixtures are normalized telemetry only.
- Regenerate fixtures and update verification notes after OSRS updates that affect Araxxor mechanics.

#### Misinterpretations To Prevent
- Wrong interpretation: partial verification is enough for normal Araxxor MVP reports.
  Correct rule: normal Araxxor reports require `VERIFIED`; otherwise T015 keeps Araxxor blocked.
- Wrong interpretation: failed verification blocks all MVP progress.
  Correct rule: Araxxor is post-MVP, so failed verification blocks only Araxxor reporting.

#### Invariants
- Fixtures contain no raw RuneLite objects, chat, raw player names, or account identifiers.
- Verification decision is explicit and reviewed.
- Unknown IDs remain debug-only unless verified and added to source-owned registries.

#### Acceptance Criteria
- [x] Verification decision records `VERIFIED` or `BLOCKED` with evidence.
- [x] Araxxor replay fixtures are anonymized normalized telemetry when verification succeeds.
- [x] `BLOCKED` documents exactly which evidence could not be verified.
- [x] `./gradlew test` passes.

#### Tests Required
- `AraxxorVerificationFixtureTest.fixtureContainsSpiderSpawnEvidenceWhenVerified()`.
- `AraxxorVerificationFixtureTest.fixtureContainsEngagementEvidenceWhenVerified()`.
- `AraxxorVerificationFixtureTest.blockedDecisionExplainsMissingEvidence()`.

#### Agent Handoff Context
This post-MVP task keeps Araxxor honest. It should never fabricate IDs or timings just to get Araxxor into normal reports.

#### Completion Notes
- Completed on branch `codex/t029-araxxor-verification-capture-fixtures`.
- Added `AraxxorVerificationDecision` with `VERIFIED`, `PARTIALLY_VERIFIED`, and `BLOCKED` states plus explicit evidence and unresolved-question lists so downstream Araxxor work can inspect a single reviewed decision.
- Recorded the current Araxxor verification review as `BLOCKED` on `2026-07-03` because official RuneLite `NpcID` sources are known, but sanitized source-owned normalized fixtures still do not prove spider spawn/availability, attack click, interaction-changed, damage, or teleport-mid-kill termination evidence.
- Added reserved placeholder files for `src/test/resources/replays/araxxor-spider-basic.jsonl` and `src/test/resources/replays/araxxor-teleport-midkill.jsonl`; they intentionally contain no committed telemetry lines until real anonymized captures exist.
- Updated `src/test/resources/replays/README.md` with the verification date, current `BLOCKED` decision, missing evidence list, and concrete capture instructions for the eventual Araxxor fixtures.
- Added `AraxxorVerificationFixtureTest` so verified-fixture assertions only activate when the decision becomes `VERIFIED`, while the current blocked path must explain the missing evidence and keep the placeholder fixtures empty.
- Validation:
  - `./gradlew test --tests "*AraxxorVerificationFixtureTest"` passed under Java 11.
  - `./gradlew test` passed under Java 11.
