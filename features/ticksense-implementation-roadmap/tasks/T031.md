### T031 - Gem Mining Metrics and Report Data

#### Goal
Generate gem mining report data from completed gem mining timelines.

#### Scope
- Create `GemMiningAnalyzer`.
- Report rock respawn-to-click latency, idle ticks, redundant clicks, movement latency, and cycle consistency.
- Provide report data consumed by the shared report model; T019 owns golden replay comparison.

#### Out of scope
- No strategy lifecycle changes unless required by a failing golden test.
- No live UI changes beyond normal report model consumption.
- No normal-user manual activity selection or live guidance.

#### Modules touched
- `src/main/java/com/ticksense/activities/gemmining/GemMiningAnalyzer.java`
- `src/main/java/com/ticksense/activities/gemmining/GemMiningReportData.java`
- `src/test/java/com/ticksense/activities/gemmining/GemMiningAnalyzerTest.java`

#### Dependencies
T016, T017, T030.

#### Related Code
- `docs/ARCHITECTURE.md` sections 15, 18, and 20.
- Planned `GemMiningAnalyzer`.

#### Implementation Touchpoints
- Read first: T030 report data and architecture "Gem Mining".
- Edit first: `GemMiningReportData.java`, then `GemMiningAnalyzer.java`.
- Symbols to search: `MetricValue`, `ActivityReport`, `GEM_ROCK_RESPAWN_TO_CLICK`.
- Required structural changes: analyzer and report data model.
- Do not edit: ID registry.
- Verify in order: `./gradlew test --tests "*GemMiningAnalyzerTest"`, `./gradlew test`.
- Stop and ask: if report wording implies RNG was execution loss.

#### Implementation Notes
- Required report labels: "Rock response", "Idle ticks", "Redundant clicks", "Movement latency", "Cycle consistency".

#### Before / After
Before:

```java
GemMiningActivityData data = strategy.buildActivityData(context, session);
```

After:

```java
ActivityReport report = gemMiningAnalyzer.buildReport(session, data, opportunities);
```

#### Migration Notes
- Metric formula updates require explicit notes because T019 golden fixtures depend on this report shape.

#### Misinterpretations To Prevent
- Wrong interpretation: hide RNG caveats to keep the report simple.
  Correct rule: separate execution metrics from RNG caveats.

#### Invariants
- Report is generated after activity completion.
- No live advice appears in report text.

#### Acceptance Criteria
- [ ] Gem mining report includes required metrics.
- [ ] Report data includes finish reason, confidence, and evidence summary from the shared report model.
- [ ] `./gradlew test` passes.

#### Tests Required
- `GemMiningAnalyzerTest.reportsLatencyAndIdleTicks()`.
- `GemMiningAnalyzerTest.separatesExecutionLossFromMiningRng()`.

#### Agent Handoff Context
This task makes gem mining readable; strategy behavior should already exist.

#### Completion Notes
- Implemented on branch `codex/t031-gem-mining-metrics-and-report-data`.
- Added `GemMiningReportData` as the typed gem-mining analyzer output that feeds the shared `ActivityReport` model without pushing report concerns back into the strategy layer.
- Added `GemMiningAnalyzer` to reconstruct completed gem-mining opportunities from open/terminal markers, compute rock response, idle ticks, redundant clicks, movement latency, cycle consistency, and an execution score, then build a shared `ActivityReport`.
- Preserved the RNG boundary by keeping depleted-rock wait in evidence/caveat text instead of rolling it into idle-tick loss, so report wording does not imply that missed gem rolls were bad execution.
- Added focused analyzer tests covering required metric labels/values plus the RNG-separation rule for tick-loss reporting.
- Local Gradle verification remains blocked in this shell because `JAVA_HOME` is unset and `java` is not on `PATH`, so GitHub Actions CI is the authoritative compile/test gate for this task.
