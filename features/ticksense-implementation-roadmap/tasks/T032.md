### T032 - Construction Strategy and Opportunities

#### Goal
Detect one verified Construction method and its menu/cadence opportunities using observe-only telemetry.

#### Scope
- Create `ConstructionStrategy` and `ConstructionState`.
- Emit `CONSTRUCTION_MENU_LATENCY`, `CONSTRUCTION_BUILD_REMOVE_CADENCE`, `CONSTRUCTION_INVENTORY_CYCLE`, and `CONSTRUCTION_BANKING_DOWNTIME`.
- Detect build/remove confirmation from menu, widget, animation, inventory, and XP evidence.

#### Out of scope
- No analyzer/report formatting; T033 owns metrics and golden reports.
- No menu mutation, generated input, or servant automation.

#### Files touched
- `src/main/java/com/ticksense/activities/construction/ConstructionStrategy.java`
- `src/main/java/com/ticksense/activities/construction/ConstructionState.java`
- `src/test/java/com/ticksense/activities/construction/ConstructionStrategyTest.java`

#### Dependencies
T008, T011, T016, T024.

#### Related Code
- `docs/ARCHITECTURE.md` sections 12 and 15.
- Planned `ConstructionStrategy`.

#### Implementation Touchpoints
- Read first: T024 verification decision and architecture "Construction".
- Edit first: `ConstructionState.java`, then `ConstructionStrategy.java`.
- Symbols to search: `CONSTRUCTION_MENU_LATENCY`, `WidgetTelemetryEvent`, `MenuInteractionTelemetryEvent`.
- Required structural changes: method-specific strategy using verified IDs.
- Do not edit: RuneLite menu entries.
- Verify in order: `./gradlew test --tests "*ConstructionStrategyTest"`, `./gradlew test`.
- Stop and ask: if T024 is not `VERIFIED`.

#### Implementation Notes
- The first implementation targets one verified method. Add variants only after duplication appears.

#### Before / After
Before:

```java
ConstructionVerificationDecision decision = ConstructionVerificationDecision.verified(...);
```

After:

```java
registry.register(new ConstructionStrategy(ConstructionIds.verified()));
```

#### Migration Notes
- Strategy remains disabled until verified method IDs exist.

#### Misinterpretations To Prevent
- Wrong interpretation: menu observation allows menu swapping.
  Correct rule: observe only; never mutate menu entries.

#### Invariants
- No generated input or menu mutation.
- Strategy consumes normalized telemetry only.

#### Acceptance Criteria
- [x] Strategy records menu-open to option-click latency.
- [x] Strategy records inventory cycle duration.
- [x] Strategy records banking/servant downtime when evidence exists.
- [x] `./gradlew test` passes.

#### Tests Required
- `ConstructionStrategyTest.recordsMenuLatency()`.
- `ConstructionStrategyTest.recordsInventoryCycleDuration()`.
- `ConstructionStrategyTest.doesNotMutateMenuEntries()`.

#### Agent Handoff Context
Keep this slice strictly observe-only; Construction is sensitive because menus are involved.

#### Completion Notes
- Completed on branch `codex/t032-construction-strategy-and-opportunities`.
- Added `ConstructionState` with verified oak-larder opportunity tracking for menu latency, build/remove cadence, inventory cycle duration, and bank-backed downtime, all derived from normalized telemetry only.
- Added `ConstructionStrategy` to start from verified build clicks, emit observe-only opportunities, terminate on verified bank-open evidence, and produce construction activity attributes for later report work.
- Registered `ConstructionStrategy` in `TickSensePlugin` only when the `ConstructionVerificationDecision` is `VERIFIED`, preserving the same verification gate pattern used by other activities.
- Added `ConstructionStrategyTest` coverage for menu latency, inventory-cycle plus banking downtime completion, and the no-menu-mutation invariant.
- Validation:
  - `./gradlew test --tests "*ConstructionStrategyTest"` passed under Java 11.
  - `./gradlew test` passed under Java 11.
