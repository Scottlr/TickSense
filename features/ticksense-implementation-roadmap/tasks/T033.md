### T033 - Construction Metrics and Golden Report

#### Goal
Generate Construction reports and golden replay coverage for the verified method.

#### Scope
- Create `ConstructionAnalyzer`.
- Report menu latency, build/remove cadence, banking downtime, and inventory cycle duration.
- Add golden report for `construction-basic.jsonl`.

#### Out of scope
- No new Construction methods.
- No live UI behavior.

#### Modules touched
- `src/main/java/com/ticksense/activities/construction/ConstructionAnalyzer.java`
- `src/main/java/com/ticksense/activities/construction/ConstructionReportData.java`
- `src/test/java/com/ticksense/activities/construction/ConstructionAnalyzerTest.java`
- `src/test/resources/golden/construction-basic-report.json`

#### Dependencies
T017, T019, T032.

#### Related Code
- `docs/ARCHITECTURE.md` sections 15, 18, and 20.
- Planned `ConstructionAnalyzer`.

#### Implementation Touchpoints
- Read first: T032 report data and architecture "Construction".
- Edit first: `ConstructionReportData.java`, then `ConstructionAnalyzer.java`.
- Symbols to search: `CONSTRUCTION_MENU_LATENCY`, `ActivityReport`, `MetricValue`.
- Required structural changes: analyzer and golden fixture.
- Do not edit: menu observation code.
- Verify in order: `./gradlew test --tests "*ConstructionAnalyzerTest"`, `./gradlew test --tests "*ReplayRegressionTest"`.
- Stop and ask: if wording implies automation advice.

#### Implementation Notes
- Labels should be player-facing: "Menu latency", "Build/remove cadence", "Banking downtime", "Inventory cycle".

#### Before / After
Before:

```java
ConstructionActivityData data = strategy.buildActivityData(context, session);
```

After:

```java
ActivityReport report = constructionAnalyzer.buildReport(session, data, opportunities);
```

#### Migration Notes
- Golden changes require formula notes.

#### Misinterpretations To Prevent
- Wrong interpretation: report should tell the user which option to choose next.
  Correct rule: reports describe completed execution only.

#### Invariants
- Report is retrospective.
- No live helper text.

#### Acceptance Criteria
- [x] Construction report includes required metrics.
- [x] Golden replay fixture matches expected report.
- [x] `./gradlew test` passes.

#### Tests Required
- `ConstructionAnalyzerTest.reportsMenuAndCycleMetrics()`.
- `ReplayRegressionTest.constructionBasicMatchesGoldenReport()`.

#### Agent Handoff Context
This is the reporting slice for an already verified strategy.

#### Completion Notes
- Completed on branch `codex/t033-construction-metrics-and-golden-report`.
- Added `ConstructionAnalyzer` and `ConstructionReportData`, wired Construction report generation, and registered Construction replay report generation in the timeline harness.
- Added Construction analyzer coverage plus a golden replay report for `construction-basic.jsonl`.
- Validation:
  - `./gradlew test --tests "*ConstructionAnalyzerTest"`
  - `./gradlew test --tests "*ReplayRegressionTest"`
  - `./gradlew test`
