### T034 - Vardorvis Strategy and Opportunities

#### Goal
Detect verified Vardorvis mechanic opportunities from normalized telemetry without live solving.

#### Scope
- Create `VardorvisStrategy` and `VardorvisState`.
- Emit only verified mechanic opportunities such as ranged-head response, blood-splat movement, axe dodge, and prayer response.
- Attribute damage during verified mechanic windows.

#### Out of scope
- No analyzer/report formatting; T035 owns metrics and golden reports.
- No projectile recoloring, live warnings, or tile/prayer prompts.

#### Modules touched
- `src/main/java/com/ticksense/activities/vardorvis/VardorvisStrategy.java`
- `src/main/java/com/ticksense/activities/vardorvis/VardorvisState.java`
- `src/test/java/com/ticksense/activities/vardorvis/VardorvisStrategyTest.java`

#### Dependencies
T008, T011, T016, T025.

#### Related Code
- `docs/ARCHITECTURE.md` sections 12 and 15.
- Planned `VardorvisStrategy`.

#### Implementation Touchpoints
- Read first: T025 verification decision and architecture "Vardorvis".
- Edit first: `VardorvisState.java`, then `VardorvisStrategy.java`.
- Symbols to search: `VARDORVIS_RANGED_HEAD_RESPONSE`, `ProjectileTelemetryEvent`, `DamageTelemetryEvent`.
- Required structural changes: mechanic opportunities gated by verification status.
- Do not edit: rendering or projectile visuals.
- Verify in order: `./gradlew test --tests "*VardorvisStrategyTest"`, `./gradlew test`.
- Stop and ask: if a mechanic is only partially verified.

#### Implementation Notes
- Missing damage is not completion evidence by itself; require a verified response signal.

#### Before / After
Before:

```java
VardorvisVerificationDecision decision = VardorvisVerificationDecision.verified(...);
```

After:

```java
registry.register(new VardorvisStrategy(VardorvisIds.verified()));
```

#### Migration Notes
- Disable unverified mechanic opportunities.

#### Misinterpretations To Prevent
- Wrong interpretation: no damage means mechanic solved.
  Correct rule: completion needs verified response evidence.

#### Invariants
- No live solver behavior.
- Only verified mechanics appear in normal reports.

#### Acceptance Criteria
- [ ] Strategy records verified mechanic opportunities.
- [ ] Strategy omits partially verified mechanics from normal reports.
- [ ] `./gradlew test` passes.

#### Tests Required
- `VardorvisStrategyTest.recordsVerifiedRangedHeadResponse()`.
- `VardorvisStrategyTest.omitsUnverifiedMechanic()`.

#### Agent Handoff Context
This is intentionally conservative. Vardorvis is high value only if the evidence is trustworthy.

