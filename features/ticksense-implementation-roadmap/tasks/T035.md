### T035 - Vardorvis Metrics and Golden Report

#### Goal
Generate Vardorvis reports and golden replay coverage from verified mechanic opportunities.

#### Scope
- Create `VardorvisAnalyzer`.
- Report response latency, damage during opportunities, downtime, and confidence.
- Add golden report for `vardorvis-basic.jsonl`.

#### Out of scope
- No new mechanic detection.
- No live UI behavior.

#### Modules touched
- `src/main/java/com/ticksense/activities/vardorvis/VardorvisAnalyzer.java`
- `src/main/java/com/ticksense/activities/vardorvis/VardorvisReportData.java`
- `src/test/java/com/ticksense/activities/vardorvis/VardorvisAnalyzerTest.java`
- `src/test/resources/golden/vardorvis-basic-report.json`

#### Dependencies
T017, T019, T034.

#### Related Code
- `docs/ARCHITECTURE.md` sections 15, 18, and 20.
- Planned `VardorvisAnalyzer`.

#### Implementation Touchpoints
- Read first: T034 activity data and architecture "Vardorvis".
- Edit first: `VardorvisReportData.java`, then `VardorvisAnalyzer.java`.
- Symbols to search: `ActivityReport`, `MetricValue`, `VARDORVIS_`.
- Required structural changes: analyzer and golden fixture.
- Do not edit: verification registry.
- Verify in order: `./gradlew test --tests "*VardorvisAnalyzerTest"`, `./gradlew test --tests "*ReplayRegressionTest"`.
- Stop and ask: if confidence wording overclaims attribution.

#### Implementation Notes
- Report confidence per mechanic when overlapping evidence makes attribution uncertain.

#### Before / After
Before:

```java
VardorvisActivityData data = strategy.buildActivityData(context, session);
```

After:

```java
ActivityReport report = vardorvisAnalyzer.buildReport(session, data, opportunities);
```

#### Migration Notes
- Golden report updates require verification notes.

#### Misinterpretations To Prevent
- Wrong interpretation: hide uncertain mechanics.
  Correct rule: report confidence and evidence, or omit unverified mechanics.

#### Invariants
- Reports are post-kill only.
- Confidence is explicit.

#### Acceptance Criteria
- [ ] Vardorvis report includes verified mechanic metrics.
- [ ] Golden replay fixture matches expected report.
- [ ] `./gradlew test` passes.

#### Tests Required
- `VardorvisAnalyzerTest.reportsVerifiedMechanicLatency()`.
- `ReplayRegressionTest.vardorvisBasicMatchesGoldenReport()`.

#### Agent Handoff Context
This task turns verified strategy outputs into readable retrospective reports.

