### T036 - Inferno Strategy and Spans

#### Goal
Detect verified Inferno attempt, wave, nibbler, prayer, supply, and death spans without live solving.

#### Scope
- Create `InfernoStrategy` and `InfernoState`.
- Emit nested spans for attempt, wave, nibbler windows, and verified prayer windows.
- Record supply usage and death timeline evidence.

#### Out of scope
- No analyzer/report formatting; T037 owns metrics and golden reports.
- No live solver or wave advice.
- No unverified prayer timing.

#### Modules touched
- `src/main/java/com/ticksense/activities/inferno/InfernoStrategy.java`
- `src/main/java/com/ticksense/activities/inferno/InfernoState.java`
- `src/test/java/com/ticksense/activities/inferno/InfernoStrategyTest.java`

#### Dependencies
T008, T011, T016, T026.

#### Related Code
- `docs/ARCHITECTURE.md` sections 11, 12, and 15.
- Planned `InfernoStrategy`.

#### Implementation Touchpoints
- Read first: T026 verification decision and architecture "Inferno".
- Edit first: `InfernoState.java`, then `InfernoStrategy.java`.
- Symbols to search: `INFERNO_WAVE`, `INFERNO_NIBBLER_WINDOW`, `INFERNO_PRAYER_WINDOW`.
- Required structural changes: nested spans driven by verified telemetry.
- Do not edit: UI to show live wave solution.
- Verify in order: `./gradlew test --tests "*InfernoStrategyTest"`, `./gradlew test`.
- Stop and ask: if prayer timing is not verified.

#### Implementation Notes
- Prayer timing remains absent unless T026 verifies prayer-state evidence.

#### Before / After
Before:

```java
InfernoVerificationDecision decision = InfernoVerificationDecision.verifiedWaveEvidence(...);
```

After:

```java
registry.register(new InfernoStrategy(InfernoIds.verified()));
```

#### Migration Notes
- Disable unverified prayer timing windows.

#### Misinterpretations To Prevent
- Wrong interpretation: wave detection should show current-wave advice.
  Correct rule: spans are for retrospective reports and replay only.

#### Invariants
- No live solver behavior.
- Nested spans are report/replay data.

#### Acceptance Criteria
- [ ] Strategy detects verified wave spans.
- [ ] Strategy records nibbler response opportunities.
- [ ] Strategy omits prayer timing when unverified.
- [ ] `./gradlew test` passes.

#### Tests Required
- `InfernoStrategyTest.recordsWaveSpanLifecycle()`.
- `InfernoStrategyTest.recordsNibblerResponseOpportunity()`.
- `InfernoStrategyTest.omitsPrayerTimingWhenUnverified()`.

#### Agent Handoff Context
Keep Inferno safely retrospective; this task should never create live solving affordances.

