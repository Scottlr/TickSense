### T037 - Inferno Metrics and Golden Report

#### Goal
Generate Inferno wave/attempt reports and golden replay coverage from verified spans and opportunities.

#### Scope
- Create `InfernoAnalyzer`.
- Report wave duration, nibbler response, verified prayer timing, supply usage, and death timeline.
- Add golden report for `inferno-wave-basic.jsonl`.

#### Out of scope
- No new Inferno detection.
- No live UI behavior.

#### Modules touched
- `src/main/java/com/ticksense/activities/inferno/InfernoAnalyzer.java`
- `src/main/java/com/ticksense/activities/inferno/InfernoReportData.java`
- `src/test/java/com/ticksense/activities/inferno/InfernoAnalyzerTest.java`
- `src/test/resources/golden/inferno-wave-basic-report.json`

#### Dependencies
T017, T019, T036.

#### Related Code
- `docs/ARCHITECTURE.md` sections 15, 18, and 20.
- Planned `InfernoAnalyzer`.

#### Implementation Touchpoints
- Read first: T036 activity data and architecture "Inferno".
- Edit first: `InfernoReportData.java`, then `InfernoAnalyzer.java`.
- Symbols to search: `INFERNO_`, `ActivityReport`, `MetricValue`.
- Required structural changes: analyzer and golden fixture.
- Do not edit: verification registry.
- Verify in order: `./gradlew test --tests "*InfernoAnalyzerTest"`, `./gradlew test --tests "*ReplayRegressionTest"`.
- Stop and ask: if prayer timing output would imply live advice.

#### Implementation Notes
- Death timeline should show last relevant completed/open opportunities and damage/supply events.

#### Before / After
Before:

```java
InfernoActivityData data = strategy.buildActivityData(context, session);
```

After:

```java
ActivityReport report = infernoAnalyzer.buildReport(session, data, opportunities);
```

#### Migration Notes
- Golden report updates require schema/verification notes.

#### Misinterpretations To Prevent
- Wrong interpretation: report should describe what the player should do on the next wave.
  Correct rule: report describes only completed execution.

#### Invariants
- Reports are retrospective.
- Unverified prayer timing is omitted.

#### Acceptance Criteria
- [ ] Inferno report includes verified wave/nibbler/death metrics.
- [ ] Golden replay fixture matches expected report.
- [ ] `./gradlew test` passes.

#### Tests Required
- `InfernoAnalyzerTest.reportsWaveAndDeathTimeline()`.
- `ReplayRegressionTest.infernoWaveBasicMatchesGoldenReport()`.

#### Agent Handoff Context
This task makes Inferno data readable without crossing into live assistance.

